rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write only for emulator or authenticated users
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
